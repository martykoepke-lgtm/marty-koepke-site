# @practical-informatics/avi

The AI Visibility Index pipeline. Importable by `apps/site` (for the customer `/scan` flow) and `apps/console` (for Marty's operator tools).

**Status:** Placeholder — populated in phase 4 of the monorepo migration. See `MIGRATION_TO_MONOREPO.md` at the repo root.

**Canon authority:** `AVI_OPERATING_STANDARD.md` at the repo root.
